1> Accessing Data:

1.1> by id:

let val=document.getElementById("hi");
console.dir(val);

>> when a id is not found it return null.

-----------------------------------------------------------------------------------------------------

1.2> by class

let val=document.getElementsByClassName("by");
console.dir(val);

>> when a class is not found it return a empty collection.

------------------------------------------------------------------------------------------------------

1.3> by tagName

let val=document.getElementsByTagName("div");
console.dir(val);

>> when a tagname is not found it return a empty collection.

-----------------------------------------------------------------------------------------------------

1.4> by using 	querySelector("#id" / ".class" / "tag");
>> it return the first element

let firstElement=document.querySelector("div");
console.dir(firstElement);

-------------------------------------------------------------------------------------------------------

1.5> 	querySelectorAll("#id" / ".class" / "tag");
>> it return the list of all nodes.

let allElements=document.querySelectorAll(".ant");
console.dir(allElements);
