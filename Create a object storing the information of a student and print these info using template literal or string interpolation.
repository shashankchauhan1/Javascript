// create a object storing the information of a student and print these info using template literal or string interpolation.

let student={
    sName:'aman',
    age:20,
    marks:98,
};
console.log(`my name is ${student.sName}, i am ${student.age} years old and my marks are ${student.marks}.`);
