// DOM : document object model

Document Object Model is a tree like structure and the parts of this dom is called node.

        			      window				        // these all are nodes.
			                |
			            document
			                |
			               html		          		this body tag is the parent of div
		                /		\			            and div is the parent of img,h1,p,etc.
		             head		body			        and img,h1,p,etc. are the siblings.
        /		/            \
meta,meta,title,link	    div      script
			                    /
			        img,h1,p,div,'tags'

>>> when a web page is loaded so the browser create the DOM(document object model) of the page.
