// write a program to find the largest value in the array using reduce method.


let arr=[1,3,5,2,4];
let largest=arr.reduce((resultVal,currentVal)=>{
    return currentVal > resultVal ? currentVal : resultVal;
});
console.log(`largest value in the array: ${arr} is ${largest}`);
